# packages/myco/src/ui-assets.generated.ts is a large base64-embedded UI
# bundle regenerated by `gen-ui-assets.ts` and deliberately committed. A
# regeneration renders as "Binary files differ" locally (git diff / git log
# -p) and collapsed on GitHub, instead of megabytes of base64 diff noise.
# Display-only — checkout content, builds, and CI are unaffected.
packages/myco/src/ui-assets.generated.ts -diff linguist-generated=true
