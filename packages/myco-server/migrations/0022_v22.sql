-- Generated by npm run migrations:emit from src/db/schema.ts (schema step 22); do not edit.

CREATE TABLE IF NOT EXISTS canopy_maps (
    project_id TEXT PRIMARY KEY CHECK (project_id NOT GLOB '*[^A-Za-z0-9._-]*' AND length(project_id) BETWEEN 1 AND 64 AND project_id NOT IN ('.', '..')) REFERENCES projects(project_id),
    revision TEXT NOT NULL,
    artifact TEXT NOT NULL,
    input_hash TEXT NOT NULL,
    repository_url TEXT NOT NULL,
    repository_branch TEXT NOT NULL,
    repository_commit TEXT NOT NULL,
    source_run_id TEXT NOT NULL,
    generated_at INTEGER NOT NULL);

INSERT OR REPLACE INTO schema_meta (key, value) VALUES ('version', '22');
