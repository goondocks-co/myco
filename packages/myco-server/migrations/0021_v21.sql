-- Generated by npm run migrations:emit from src/db/schema.ts (schema step 21); do not edit.

CREATE TABLE IF NOT EXISTS project_repositories (
    project_id TEXT PRIMARY KEY CHECK (project_id NOT GLOB '*[^A-Za-z0-9._-]*' AND length(project_id) BETWEEN 1 AND 64 AND project_id NOT IN ('.', '..')) REFERENCES projects(project_id),
    revision TEXT NOT NULL,
    url TEXT NOT NULL,
    branch TEXT NOT NULL,
    username TEXT,
    secret_slot TEXT,
    updated_at INTEGER NOT NULL,
    updated_by TEXT NOT NULL,
    CHECK ((secret_slot IS NULL AND username IS NULL) OR (secret_slot IS NOT NULL AND username IS NOT NULL)));

INSERT OR REPLACE INTO schema_meta (key, value) VALUES ('version', '21');
