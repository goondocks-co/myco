-- Generated by npm run migrations:emit from src/db/schema.ts (schema step 44); do not edit.

CREATE TABLE IF NOT EXISTS worker_contacts (
     credential_id TEXT PRIMARY KEY REFERENCES member_credentials(id),
     machine_id    TEXT,
     offers        TEXT,
     capabilities  TEXT,
     last_reason   TEXT,
     last_seen_at  INTEGER NOT NULL,
     updated_at    INTEGER NOT NULL);

CREATE INDEX IF NOT EXISTS idx_worker_contacts_seen ON worker_contacts (last_seen_at);

INSERT OR REPLACE INTO schema_meta (key, value) VALUES ('version', '44');
