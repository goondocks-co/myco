/**
 * The per-tier value shapes of a provider override, re-exported from the shared
 * package for the task loader; `config/schema.ts` reads them from the shared
 * package directly.
 */
export { ThinkingBudgetValueSchema, EffortValueSchema } from '@goondocks/myco-shared/agent-config';
