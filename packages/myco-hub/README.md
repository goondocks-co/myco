# Myco Hub

Local operator hub for switching between and managing per-project Myco daemons.

```sh
myco-hub install
myco-hub open
```

The hub binds to `127.0.0.1:21000` by default and proxies project dashboards
under `/p/<project-id>/`.
